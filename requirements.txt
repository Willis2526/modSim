# Pinned to the latest versions verified working as of 2026-08-26.
# When bumping: install the latest of each, run the test suite, then update
# these pins to match — don't leave them unpinned or let them go stale.
pymodbus==3.15.0
fastapi==0.141.1
uvicorn==0.52.4
aiofiles==25.1.0

# Testing dependencies
pytest==9.1.1
pytest-cov==7.1.0
httpx==0.28.1
